<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mink.springtest.mybatis.repository.RealEstateRepository">
    <select id="selectRealEstate"
            parameterType="int"
            resultType="com.mink.springtest.mybatis.domain.RealEstate">

            SELECT
                `id`
                ,`realtor_id`
                ,`address`
                ,`area`
                ,`type`
                ,`price`
                ,`rent_price`
                ,`created_at`
                ,`updated_at`
            FROM
                `real_estate`
            WHERE
                `id` = #{id}
    </select>

    <select id="selectRealEstateListByRentPrice" parameterType="int" resultType="com.mink.springtest.mybatis.domain.RealEstate">

        SELECT
            `id`
             ,`realtor_id`
             ,`address`
             ,`area`
             ,`type`
             ,`price`
             ,`rent_price`
             ,`created_at`
             ,`updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[
            `type`='월세'
            AND `rent_price` <  #{rentPrice}
            ]]>
    </select>

    <select id="selectRealEstateListByAreaPrice" parameterType="map" resultType="com.mink.springtest.mybatis.domain.RealEstate">

        SELECT
            `id`
             ,`realtor_id`
             ,`address`
             ,`area`
             ,`type`
             ,`price`
             ,`rent_price`
             ,`created_at`
             ,`updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[
            `area`>=#{area}
          AND `price` <=  #{price}
        ]]>
    </select>

    <insert id="insertRealEstateByObject" parameterType="com.mink.springtest.mybatis.domain.RealEstate">
        INSERT INTO
            `real_estate`
        (
            `realtor_id`
            ,`address`
            ,`area`
            ,`type`
            ,`price`
        )
        VALUE
        (
            #{realtorId}
            ,#{address}
            ,#{area}
            ,#{type}
            ,#{price}
        )
    </insert>


    <insert id="insertRealEstate" parameterType="map">
        INSERT INTO
            `real_estate`
        (
          `realtor_id`
        ,`address`
        ,`area`
        ,`type`
        ,`price`
        ,`rent_price`
        )
        VALUES
            (
            #{realtorId}
            ,#{address}
            ,#{area}
            ,#{type}
            ,#{price}
            ,#{rentPrice}
            )
    </insert>

</mapper>